# Database — use the non-superuser application role so RLS applies.
# The vivfy superuser exists for migrations only; never put it here.
DATABASE_URL="postgresql://vivfy_app:vivfy_app@localhost:5432/vivfy?schema=public"

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# App
PORT=3000
NODE_ENV=development
